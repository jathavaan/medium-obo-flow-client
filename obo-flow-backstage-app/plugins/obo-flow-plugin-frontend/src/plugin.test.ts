import { oboFlowPluginFrontendPlugin } from './plugin';

describe('obo-flow-plugin-frontend', () => {
  it('should export plugin', () => {
    expect(oboFlowPluginFrontendPlugin).toBeDefined();
  });
});
